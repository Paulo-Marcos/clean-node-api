{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --noStackTrace --colors --passWithNoTests",
    "test:unit": "npm test -- --watch -c jest-unit-config.js",
    "test:integration": "npm test -- --watch -c jest-test-config.js",
    "test:staged": "npm test --  --findRelatedTests ",
    "test:ci":"npm test --  --coverage",
    "pre-commit": "lint-staged"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Paulo-Marcos/clean-node-api.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Paulo-Marcos/clean-node-api/issues"
  },
  "homepage": "https://github.com/Paulo-Marcos/clean-node-api#readme",
  "devDependencies": {
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "lint-staged": "^11.0.0",
    "standard": "^16.0.3"
  }
}
